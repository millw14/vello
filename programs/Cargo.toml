[workspace]
members = [
    "velo_mixer",
    "velo_private_tx", 
    "velo_subscription",
    "velo_stealth"
]

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1

[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1
